const express = require("express"); const app = express(); app.use(express.json()); app.get("/health", (req, res) => res.json({status: "OK", message: "Ascend API is running!"})); app.post("/api/auth/register", (req, res) => res.json({success: true, data: {user: {id: "demo-user", email: req.body.email}, tokens: {accessToken: "access_demo_123"}}})); app.listen(3000, () => console.log("🚀 Ascend API running on port 3000"));
